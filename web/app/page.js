"use client";
import React from 'react';

export default function App({ children }) {
  // Removed login logic, no longer needed here since handled in DashboardLayout
  return (
    <div className="flex">Welcome</div>
  );
}